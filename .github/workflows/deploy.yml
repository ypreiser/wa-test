name: Deploy and Run WA Connection Test

on:
  push:
    branches: ["master"]

jobs:
  deploy-and-test:
    name: Deploy to Server and Run Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Sync Files to Server
        uses: easingthemes/ssh-deploy@main
        with:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_KEY }}
          REMOTE_HOST: ${{ secrets.SSH_HOST }}
          REMOTE_USER: ${{ secrets.SSH_USER }}
          SOURCE: "./"
          TARGET: "~/wa-test"
          EXCLUDE: "/.github/, /.git/"

      - name: Build and Run Test on Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            set -e
            echo "✅ Logged into server. Navigating to deployment directory..."
            cd ~/wa-test

            echo "🛑 Stopping and removing any old test resources..."
            docker compose -f docker-compose.test.yml down --volumes || true

            # --- FIX: Forcefully remove old session data to prevent permission errors ---
            # We use sudo because the directory is owned by the root user.
            echo "🔑 Clearing old session data on host to ensure a clean build context..."
            sudo rm -rf wwebjs_auth_test

            echo "🚀 Building and running the test. Logs will stream below:"
            docker compose -f docker-compose.test.yml up --build
